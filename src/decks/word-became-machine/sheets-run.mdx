{/* _class: hero */}

a page of **token pairs** --- a word, and the word before it

![bg](./assets/bg-cutouts-overview.avif)

```notes
- hold one up: boxed token = the word before, beside it = what followed; colour per word
- a token is one word or punctuation mark (say once, move on)
- shuffled, so nobody's holding a story
- ten seconds to scan, then eyes up
```

---

## the algorithm

![bg right:40%](./assets/bg-algorithm-generation.avif)

1. I **call out** the last token we wrote (and its colour)
2. **hands up** if it's the _boxed_ token on your page
3. I **pick a hand at random** --- you read out the token beside it
4. I **write it down**, and we go again

```notes
- walk it once, slowly
- one hand each; multiple matches, go with the first
- **random** pick, not fastest --- that's what makes it the model
- seed: someone reads any pair off their sheet, boxed word first; take whatever they give
```

---

```comment
The board slide. No words and no `.hero` class: the artwork is the deck's
own whiteboard surface (the same asset the board paints, on the same dark
ground), so the slide reads as a blank board with the timer sitting on it.
Write on it with the annotation layer (⇧W), NOT the board itself (W) ---
the board is an opaque layer over the slide and would take the timer with
it.
```

![bg](./assets/deco-whiteboard.avif)

<Timer minutes={5} overlay />

```notes
- Shift-W (annotation), NOT W; write the two seed words BIG, call the second, go
- 5 min, 12--15 tokens, no commentary between hands and word
- once each: hands are the distribution (common word = forest, rare = two); the pick is a coin toss (= regenerate button)
- empty seat means those pairs are gone: say so, reseed from `the`/`he`/`Word`, not a full stop
- stop while they want more; read it back
```

---

{/* _class: hero */}

we just ran a **language model**

![bg](./assets/bg-div-reception.avif)

```notes
- say it flatly and let it sit: the actual mechanism, slow and small enough to see
```

---

## what role did you play?

![bg right:40%](./assets/bg-core-insight-portrait.avif)

each word on your sheet → a **token**

the pairs on your page → the **parameters** of the model

the hands in the air → the **probability distribution** over next tokens

my random pick → **sampling**

the sentence I wrote → **generation**

```notes
- jargon now has something physical behind it; go at a fair clip
- parameters = weights; "a trillion parameters" = a big pile of what's on your lap
- say it: between you, you're holding 1,266 of them
```
