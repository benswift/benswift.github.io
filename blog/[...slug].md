---
layout: doc
aside: false
---

<script setup>
import { useData } from 'vitepress'
const { frontmatter } = useData()
</script>

# {{ $frontmatter.title }}

<p class="post-date">{{ $frontmatter.dateFormatted }}</p>

<p v-if="$frontmatter.subtitle" class="post-subtitle">{{ $frontmatter.subtitle }}</p>

<TagList v-if="$frontmatter.tags?.length" :tags="$frontmatter.tags" />
